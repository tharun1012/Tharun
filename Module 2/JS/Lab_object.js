let pet = {
    type: "German shepherd",
    name: "charlie",
    legs: "four",
    color: "brown"
};

for (let i in pet) {
    console.log(pet[i]);
}
let food = {
    type: "Vegetarian",
    favorite:"Dosa"
};

for (let i in food) {
    console.log(food[i]);
}

